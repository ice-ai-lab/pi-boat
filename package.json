{
  "name": "pi-boat",
  "private": true,
  "packageManager": "pnpm@12.4.2",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "dev": "turbo run dev --filter=@ice-ai/web --filter=@ice-ai/server",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "typecheck": "turbo run typecheck",
    "clean": "git clean -xdf",
    "dev:libs": "turbo run dev --filter=@ice-ai/protocol --filter=@ice-ai/core --filter=@ice-ai/client --filter=@ice-ai/ui"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.14",
    "turbo": "^2.10.13",
    "typescript": "~7.0.2"
  }
}
