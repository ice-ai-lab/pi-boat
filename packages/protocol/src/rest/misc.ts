/** GET /api/health —— 服务健康检查（M0 已落地） */
export type HealthResponse = {
  ok: boolean;
  name: string;
};

// ---------------------------------------------------------------------------
// ⑦ 辅助通道（docs/02 §7）：liveness lease
// ---------------------------------------------------------------------------
